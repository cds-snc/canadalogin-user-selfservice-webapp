{
  "id": "0ea1f92c",
  "title": "Initial: Extract type definitions from constants and tests",
  "tags": [
    "frontend",
    "typescript",
    "migration",
    "chore"
  ],
  "status": "open",
  "created_at": "2026-02-26T15:54:44.839Z"
}

What I did

- Scanned frontend/src and created initial TypeScript types derived from existing JS constants and test fixtures.
- Added files:
  - frontend/src/types/api.ts
  - frontend/src/types/user.ts
  - frontend/src/types/constants.ts
  - frontend/src/types/index.ts

Why

- These domain-level types (API responses, user/profile shapes, flow types) make future conversions easier and reduce churn when converting components/services to TS.

Next steps

- Expand types by scanning service files (frontend/src/services), utils (frontend/src/utils), and components for prop shapes and JSDoc/PropTypes.
- Create automated inventory (list of .js/.jsx files grouped by complexity) to plan conversion batches.
- Add codemods/scripts to safely rename files and add type stubs where necessary.

Acceptance criteria

- Generated type files exist and compile (no syntax errors).
- Types cover majority of domain objects used by services and components (user, profile, API responses, flow types).

Notes

- This is a starting point; many files still need specific types (component props, hook return types, API payload shapes). We'll extract those incrementally.
