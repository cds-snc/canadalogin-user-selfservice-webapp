{
  "id": "a89ed09e",
  "title": "Batch E — Convert routes/pages and app entry (PR 5)",
  "tags": [
    "migration",
    "batch-e",
    "typescript"
  ],
  "status": "open",
  "created_at": "2026-02-26T16:15:06.858Z"
}

Scope (Batch E)

Convert routing and top-level pages after core modules are typed:

- src/routes.jsx -> src/routes.tsx
- src/router.jsx -> src/router.tsx
- Ensure main.tsx imports extensionless paths and all converted files are referenced correctly

Prerequisites

- Batches A-D completed

Steps

1. Branch: feat/ts/batch-e-routes
2. Rename files, add route type definitions where needed
3. Update imports and run build
4. Validate app runs in dev (vite) and production build

Acceptance criteria

- Build completes
- App runs locally with dev server (npm --prefix frontend run dev)

Estimated effort: 1–3 days
