{
  "id": "420a376e",
  "title": "POC: Convert fetchUserFIDO2Credentials to TypeScript and validate build",
  "tags": [
    "frontend",
    "typescript",
    "migration",
    "poc"
  ],
  "status": "open",
  "created_at": "2026-02-26T15:58:34.343Z"
}

What I did

- Renamed and converted:
  - frontend/src/features/ManageFIDO2/utils/fetchUserFIDO2Credentials.js -> frontend/src/features/ManageFIDO2/utils/fetchUserFIDO2Credentials.ts
  - Added type FetchFIDO2Options and typed the exported async function.

Files changed

- frontend/src/features/ManageFIDO2/utils/fetchUserFIDO2Credentials.ts (new)
- original file was moved (git mv) in the working tree

Why

- This is a low-risk, small utility with clear input/output and a good candidate for a first conversion.

Build check

- I attempted to run `npm run build` in frontend, but the environment does not have dev dependencies installed (tsc not found). Locally / CI you should run:
  - npm ci (or npm install)
  - npm --prefix frontend run build

If build fails after running locally, inspect TypeScript errors; it's acceptable to add `// @ts-ignore` or `: any` placeholders to unblock the build and iterate.

Next

- If you want, I can continue converting the next batch (utils -> small presentational components) or revert this file change and instead create a .d.ts stub for it (safer).

Acceptance criteria

- `npm --prefix frontend run build` completes successfully after running npm install.
- No runtime regressions; behavior unchanged.
