# Environment variables for Playwright E2E tests.
# Points the frontend at localhost so all API calls are intercepted
# by Playwright's page.route() without CORS issues.
VITE_BACKEND_API_URL=http://localhost:3000
VITE_ENVIRONMENT=dev
